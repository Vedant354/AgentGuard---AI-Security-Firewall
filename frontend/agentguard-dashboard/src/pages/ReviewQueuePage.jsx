import { PagePlaceholder } from '../components/PagePlaceholder';

export function ReviewQueuePage() {
  return <PagePlaceholder eyebrow="Human oversight" title="Review Queue" description="Pending decisions will be surfaced here for analyst review and explicit allow or block action." actionLabel="No review queue UI has been connected yet." />;
}
